/* Web3Forms form identifier for the verified business email (safe for public use).
   Add the key from https://app.web3forms.com/ and enable hCaptcha in that form's
   dashboard. Without a key, the page offers telephone and email contact. */
window.ATOZ_CONTACT = Object.freeze({
  accessKey: ""
});
